<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aiqing.wallet">

    <!-- 访问网络状态 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 控制呼吸灯，振动器等，用于新消息提醒 -->
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 外置存储存取权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- 多媒体相关 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- 如果需要实时音视频通话模块，下面的权限也是必须的。否则，可以不加 -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.BROADCAST_STICKY" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION" /> -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_LOGS" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:name=".App"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <receiver
            android:name=".download.DownloadProcessReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.DOWNLOAD_COMPLETE" />
            </intent-filter>
        </receiver>

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.aiqing.wallet.provider"
            android:exported="false"
            android:grantUriPermissions="true">

            <!-- 元数据 -->
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS2"
                android:resource="@xml/file_paths" />
        </provider>

        <activity android:name=".splash.SplashAct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".home.ActiveFriendAct" />
        <activity android:name=".my.PersonalDataActivity" />
        <activity android:name=".my.securitycenter.SecurityCenterActivity" />
        <activity android:name=".my.securitycenter.ModifyPhoneActivity" />
        <activity android:name=".my.securitycenter.ModifyLoginPwActivity" />
        <activity android:name=".my.securitycenter.ModifyTransactionPwActivity" />
        <activity android:name=".my.securitycenter.RetrieveTransactionPwActivity" />
        <activity android:name=".my.certification.CertificationActivity" />
        <activity android:name=".my.certification.PrimaryCertificationActivity" />
        <activity android:name=".my.certification.SeniorCertificationActivity" />
        <activity android:name=".my.invitation.FriendInvitationActivity" />
        <activity android:name=".my.invitation.MyInvitationsListActivity" />
        <activity android:name=".login.LoginAct" />
        <activity android:name=".login.ForgetPassAct" />
        <activity android:name=".login.RegisterAct" />
        <activity android:name=".login.ProtocolAct" />
        <activity android:name=".home.transferaccounts.TransferAccountsActivity" />
        <activity android:name=".home.transferaccounts.ConfirmTransferActivity" />
        <activity android:name=".home.details.DetailsActivity" />
        <activity android:name=".home.transferaccounts.AccountBookActivity" />
        <activity android:name=".wallet.MoneyDetailsActivity" />
        <activity android:name=".wallet.RechargeActivity" />
        <activity android:name=".wallet.ConfirmMoneyActivity" />
        <activity android:name=".wallet.WithdrawMoneyActivity" />
        <activity android:name=".home.details.TradeRecordAct" />
        <activity android:name=".home.notice.NoticeListAct" />
        <activity android:name=".home.notice.DetailsAct" />
        <activity android:name=".firend.FriendActivity" />
        <activity android:name=".my.AboutUsAct" />
        <activity android:name=".home.BonusAct" />
        <!--
         APP key, 可以在这里设置，也可以在 SDKOptions 中提供。
         如果 SDKOptions 中提供了，取 SDKOptions 中的值。
        -->

        <meta-data
            android:name="com.netease.nim.appKey"
            android:value="0ab199e9941702ea6d45c8261f89b645" />

        <!-- 云信后台服务，请使用独立进程。 -->
        <service
            android:name="com.netease.nimlib.service.NimService"
            android:process=":core" />

        <!-- 云信后台辅助服务 -->
        <service
            android:name="com.netease.nimlib.service.NimService$Aux"
            android:process=":core" />

        <!-- 云信后台辅助服务 -->
        <service
            android:name="com.netease.nimlib.job.NIMJobService"
            android:exported="true"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:process=":core" />

        <!-- 云信监视系统启动和网络变化的广播接收器，保持和 NimService 同一进程 -->
        <receiver
            android:name="com.netease.nimlib.service.NimReceiver"
            android:exported="false"
            android:process=":core">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <!-- 云信进程间通信 Receiver -->
        <receiver android:name="com.netease.nimlib.service.ResponseReceiver" />

        <!-- 云信进程间通信service -->
        <service android:name="com.netease.nimlib.service.ResponseService" />

        <!-- 云信进程间通信provider -->
        <!-- android:authorities="{包名}.ipc.provider", 请将com.netease.nim.demo替换为自己的包名 -->
        <provider
            android:name="com.netease.nimlib.ipc.NIMContentProvider"
            android:authorities="com.aiqing.wallet.ipc.provider"
            android:exported="false"
            android:process=":core" />

        <activity android:name=".home.information.InformationActivity" />
        <activity android:name=".home.node.NodePlanActivity" />
        <activity android:name=".home.node.ParticipationNodeActivity" />
        <activity android:name=".home.node.NodeDetailsActivity"></activity>
    </application>

</manifest>